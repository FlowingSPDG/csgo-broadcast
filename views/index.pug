<!DOCTYPE html>
html
head
  meta(charset="UTF-8")
  title(Plays)
body
  table
    thead
      tr
        th Play
        td timestamp
        td auth
    tbody
      //{% for show in shows %}
      tr
        th
          a(href="steam://rungame/730/76561202255233023/+playcast%20%22http://csgo.joke-lan.net:3000/match/{{show.token}}")
            //{{show.token}}
        td({{show.timestamp}})
        td({{show.auth}})
      //{% endfor %}